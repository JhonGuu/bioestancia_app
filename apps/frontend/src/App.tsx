// Este archivo viene del scaffold inicial de Vite y no se pudo borrar desde
// acá (el mount del sandbox no permite eliminar los archivos que trajo
// `create-vite`). No se usa: el punto de entrada real es `main.tsx`, que
// monta `AuthProvider` + `QueryClientProvider` + `RouterProvider` directo.
// Podés borrar este archivo (y `App.css`) sin problema desde tu editor.
export {};
